# ASD
 
This was the final project. 

The covered modules where: linear regression, logistic regression, LDA, QDA, re-sampling methods, model selection (subset selection and shrinkage), SVM, KNN and trees.

PS: we all make mistakes, that's how we learn. 
